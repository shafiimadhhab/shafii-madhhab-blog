# Tina client ID from your TinaCloud project's Overview tab.
PUBLIC_TINA_CLIENT_ID=your_tina_client_id

# Optional alias if you prefer a non-public-looking variable name in CI.
# TINA_CLIENT_ID=your_tina_client_id

# Tina read-only token from your TinaCloud project's Tokens tab.
TINA_TOKEN=your_tina_token

# Tina search indexer token from your TinaCloud project's Search tab.
# Without this, TinaCMS content search stays disabled and builds skip indexing.
TINA_SEARCH_TOKEN=your_tina_search_token

# Optional alias if you prefer the more explicit name in CI.
# TINA_SEARCH_INDEXER_TOKEN=your_tina_search_token
